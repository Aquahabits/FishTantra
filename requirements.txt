# ---------- Core Web ----------
flask==3.0.0
gunicorn==21.2.0

# ---------- ML (CPU-ONLY to avoid OOM on Render) ----------
torch==2.0.1+cpu
torchvision==0.15.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# ---------- Image Processing ----------
pillow==10.2.0

# ---------- Scientific ----------
numpy==1.26.4

# ---------- PDF Generation ----------
reportlab==4.0.9
